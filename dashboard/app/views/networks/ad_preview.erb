<!-- XXX lots of duplication between /networks/ad_preview & /publishers/ad_preview -->

<% title "Network - " + @network.network_name %>

<h2>Currently <%= @tags.size %> enabled tags</h2>

<% for tag in @tags %>
    <strong><%= link_to "Tag - " + tag.tag_name, tag, :action => :show %></strong><br>
    <iframe  src="<%= tag.preview_url %>" style="<%= tag.css_size %>;border:1px dashed #660099"></iframe>
    <p>
<% end %>
